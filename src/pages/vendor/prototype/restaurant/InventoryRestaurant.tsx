import { PrototypeInventoryManager, restaurantPlugin } from '@/features/floor-plan';

export default function PrototypeInventoryRestaurant() {
  return <PrototypeInventoryManager plugin={restaurantPlugin} />;
}
