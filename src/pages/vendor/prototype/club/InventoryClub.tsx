import { PrototypeInventoryManager, nightclubPlugin } from '@/features/floor-plan';

export default function PrototypeInventoryClub() {
  return <PrototypeInventoryManager plugin={nightclubPlugin} />;
}
